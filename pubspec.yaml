name: gowes_pulen
description: Aplikasi Komunitas & Perpesanan Sepeda Gowes Pulen Ultimate.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.0.0
  firebase_database: ^11.0.0
  firebase_auth: ^5.0.0
  firebase_messaging: ^15.0.0
  google_sign_in: ^6.2.0
  shared_preferences: ^2.2.0
  image_picker: ^1.1.0
  file_picker: ^10.3.2
  geolocator: ^12.0.0
  record: ^7.1.1
  flutter_image_compress: ^2.3.0
  flutter_map: ^7.0.0
  latlong2: ^0.9.0
  url_launcher: ^6.3.0
  photo_view: ^0.15.0
  gpx: ^2.2.0
  audioplayers: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true






